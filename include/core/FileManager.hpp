#pragma once
#include <fstream>
namespace se4 {
	const auto SAVE_DIRECTORY = "save/save.txt";

	class FileManager {
	public:
		FileManager() = default;
		virtual ~FileManager() = default;


	private:

	};
}